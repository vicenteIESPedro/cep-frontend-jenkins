pipeline {
    agent any

    stages {
        stage('inicial'){
            steps {
                sh 'ls -alh'
                sh 'node --version'
                sh 'npm --version'  
            }
         
        }
    }
}